i want to have a replica of the copernicus climate data store but for rfs v3 data. please browse a few of the cds pages such as https://cds.climate.copernicus.eu/datasets/reanalysis-era5-single-levels-timeseries?tab=download and https://cds.climate.copernicus.eu/datasets. refer to the style of /Users/rchales/code/geoglowsapps/webapp-rfs-v3. the choices for datasets should be categorized as hydrography with subentries for streams, catchments, confluence points, lakes. then retrospective with subcategories for hourly, daily, monthly, yearly, return periods, annual maximums (hourly) annual maximums (daily). then forecasts with options for the date of the forecast initialization and whether to obtain the full ensemble forecast or just the simplified forecast. i want the outputs ouputs to include zarr for timeseries or parquet for the hydrography. we can also offer the command to download it yourself if you want to get it all from aws s3 buckets using s5cmd or awscli. i want to have a column on the right like copernicus CDS with the information about the dataset like its last updated date, original creation date, a changelog, license terms, etc. This app should require login to download. the app should present a map interface to help people identify the area of interest using something in the style of the hydrography exploration tools for selecting ranges of rivers between 2+ points or whole watersheds. users should be forced to sign in and accept terms and conditions before downloading using the geoglows-auth package. for now, this can be a dummy and pressing sign in automatically handles everything. i want breadcrumbs on the top showing how you've drilled down to the datasets available. i want the datasets available to have a top level distinction for model version. versions 1 and 2 should only allow browsing available data and provide alternaitve instuctions to obtain them and not attempt to actually execute the downloads via the browser. you should use the js-riverforecastsystem package which the reference v3 app uses. it should be a vite app. it should prefer javascript and no framework. it should be designed to be built like an app inside the apps.geoglows portal the same way the reference app is. it should show the geoglows logo as a way to return to the / level. this app's path should be /rfs-data-store. the updated and creation date information can be dummy variables for now marked as a todo item but those will be eventually pulled on demand from the s3 buckets from a sentinel json file.
